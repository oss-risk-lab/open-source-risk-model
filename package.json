{
  "private": true,
  "scripts": {
    "test:ui": "node --experimental-vm-modules node_modules/.bin/vitest run test/ui/ --reporter=verbose"
  },
  "devDependencies": {
    "fast-check": "^3.15.0",
    "jsdom": "^24.1.3",
    "vitest": "^1.6.0"
  }
}
